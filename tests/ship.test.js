// const Ship = require('../components/Ship');
// const isSunk = require('../components/ship');
// const hit = require('../components/ship');

// test('Creates a ship object', () => {
//   expect(Ship(4)).toEqual({
//     length: 4,
//     shipLayout: [undefined, undefined, undefined, undefined],
//   });
// });

// test('Checks if ship is sunk', () => {
//   expect(isSunk()).toBe(false);
// });

// test('Checks if ship is hit', () => {
//   expect(hit(2)).toStrictEqual([undefined, undefined, 'x', undefined]);
// });
